#include <stdio.h>
#include <stdlib.h>

int main(void){
    int col, row;
    int **dpa;

    printf("row: ");
    scanf("%d", &row);
    printf("col: ");
    scanf("%d", &col);

    dpa = (int *)malloc(sizeof(int)*col);

         

    return 0;
}